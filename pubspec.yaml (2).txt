name: ethio_shop
description: The ETHIO🛍 Adaptive Marketplace application.
publish_to: 'none'
version: 1.0.0+1

environment:
  # Ensure the SDK supports Dart's latest features and null safety
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 🎨 Icons
  cupertino_icons: ^1.0.6

  # 🔑 Firebase Packages (Updated to latest stable versions)
  # ... other packages
  # 🔑 Firebase Packages (SYNCHRONIZED TO THE LATEST STABLE SET)
  # cloud_firestore: 6.1.0 is compatible with firebase_core and dart >=3.2.0
  
# 🔑 Firebase Packages

  firebase_core: ^3.1.1

  firebase_auth: ^5.1.1

  cloud_firestore: ^5.0.2

  firebase_storage: ^12.1.1


  # ... rest of your dependencies  # ... rest of your dependencies     # New: ^11.5.6 -> ^12.1.0

  # 🗺 Maps & Location (Updated to latest stable versions)
  google_maps_flutter: ^2.7.0
  geolocator: ^10.1.0           # Current version is fine

  # 🛠 Utilities (Updated lottie and connectivity_plus)
  connectivity_plus: ^6.0.3
  lottie: ^3.1.2
  
  # 🖼 Media
  image_picker: ^1.0.4
  cached_network_image: ^3.3.0
  intl: ^0.18.1
  url_launcher: ^6.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  # ✅ Asset paths look correct
  assets:
    - assets/lottie/
    - assets/icons/
    - assets/branding/
