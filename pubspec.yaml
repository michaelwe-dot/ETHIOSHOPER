name: ethio_shop
description: The ETHIO🛍 Adaptive Marketplace application.
publish_to: 'none'
version: 1.0.0+1

environment:
  # Ensure the SDK supports Dart's latest features and null safety
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 🎨 Icons
  cupertino_icons: ^1.0.6

  # 🔑 Firebase Packages (Updated to latest stable versions)
  firebase_core: ^4.2.1         # New: ^2.24.2 -> ^4.2.1
  firebase_auth: ^6.1.2         # New: ^4.15.2 -> ^6.1.2
  cloud_firestore: ^5.2.1       # New: ^4.13.5 -> ^5.2.1
  firebase_storage: ^12.1.0     # New: ^11.5.6 -> ^12.1.0

  # 🗺 Maps & Location (Updated to latest stable versions)
  google_maps_flutter: ^2.14.0  # New: ^2.5.0 -> ^2.14.0
  geolocator: ^10.1.0           # Current version is fine

  # 🛠 Utilities (Updated lottie and connectivity_plus)
  connectivity_plus: ^6.0.2     # New: ^5.0.2 -> ^6.0.2
  lottie: ^3.1.2                # New: ^3.1.0 -> ^3.1.2
  
  # 🖼 Media
  image_picker: ^1.0.4
  cached_network_image: ^3.3.0
  intl: ^0.18.1
  url_launcher: ^6.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  # ✅ Asset paths look correct
  assets:
    - assets/lottie/
    - assets/icons/
    - assets/branding/
